from setuptools import setup

setup(
   name='Distributions',
   version='0.1.0',
   author='Narayan Sharma',
   packages=['Distributions'],
   zip_safe= False,
   description='An awesome package that makes working with distributions very easy',
   
)